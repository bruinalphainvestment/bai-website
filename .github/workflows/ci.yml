name: CI

on:
  pull_request:
  push:
    branches-ignore:
      - 'dependabot/**'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-checks:
    name: Typecheck + Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Sanity-managed content audit
        run: bunx playwright test tests/editable-content.spec.ts --project=chromium-desktop --grep "hardcoded|queried and seeded|committee detail visible content|preserve seed"
